import { Anuncio } from '../types';

export const ScrapingService = {
  async scrapeAnuncios(): Promise<Anuncio[]> {
    // Implementar la lógica de scraping aquí
    // Por ahora, retornamos un array vacío
    return [];
  }
};